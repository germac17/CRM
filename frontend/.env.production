# URL бэкенда с API (обязательно для Vercel: https://ваш-api.ru или в Vercel → Env → VITE_API_URL).
# Пусто = тот же origin, что и страница (только если API на том же домене, не только статика на Vercel).
VITE_API_URL=
